23:41
yeah, i was able to get clients to 
send GameMessages over the network
by simply specifing the recipeint and message type 
in the msg the client want's to send. This is horrible
as this is supposed to be taken care of at the 
api boundary, not ux concern. BUT, it's good enough 
for now, infact, it's slightly convincing me to use 
packet framing, as i think it allows for more dynamic 
cases, although more teidous, as compared  to using
json. Or maybe this might be an oppurtunity to fully 
explore protobufs? I'll see what it can do

But atm, I want the client to include their ssids for 
the game, so the srver can reference their play session
I think that's easier. So now, the server just appends
the ssid into the NewGameMessage content, using ';'
as a delimeter, and ssid as the 'key' if you may.
So the client sees something like this:
	 ```
	 Message {
			 MessageType: NewGameMessage,
			 From: secretServerId,
			 Dest: your-future-uuid,
			 Content: "Game starting;ssid928018u0=j23",
	 }
	 ```

	 And the server
	 ```
	 Messagee {
			 From: you,
			 Dest: away,
			 Content: "your-play;ssid",
			 MessageType: GameMessage
	 }
	 ```

Violation of these rules will automatically declare
the other player winner, easier that way atm

And slowly, we can see how Protobufs, or binary framing look 
more like the appealing solution, otherswise, we'd need 
excessive tracking and almost sentience for our app to 
know when to switch btwn normal message or game message

TODO-LATER:
- Client should append their ssid for every GameMessage they send


Now that we've switched to Protobufs, we need 
to determine a way to just IDENTIFY users now!!

Or wait, we can just auth them with an Auth Packet
Packet {
	int32 from;
	int32 to;
	Auth: {
		string username;
		...some other credentials
	}
}



===================
Friday 6th March
===================
Now we've agreed on a simple handshake
where the client sends an auth packet upon 
first connection. 
[] TODO set deadlines between io/network operations


=============
		SCOPE 
=============
	So let's see our scope for today...
	We want the client to send a newGame Message as:
		ng user-name

	And then the server can parse this message and 
	confirm the reciepient honours this request.
	For now, that impl should not be a priory but it
	shoudl have a stub in the code just to understand the flow


	And then we create a session for both of them where they we
	can add some game logic

=============================
* HIGHLEVEL SERVER IMPL
=============================
	When a user sends a NG packet, the server 
	should create a 'session' and notifies the 
	recipeient in the NG Packet that a GAME is 
	about to start. 


* Questions
1. Should we make the client automatically just send GamePackets 
   after a new game session has started? Instead of parsing every 
	 input and deciphering what kind of Packet is should send? 
	 And if so, we'd have to deal with 'state' again. Like 'currentState'?
	 Since the client doesn't use a ton of resources and is simple 
	 and straight forward compared to the server.
	 --- Scenario Analysis:
				If a new NewGamePacket was recieved from the server, we automatically 
				send GamePackets till the user quits or when the server decides to 
				end the game.

